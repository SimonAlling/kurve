"use strict";

const text = Object.freeze({
    hint_pick: `Pick your desired color by pressing the corresponding LEFT key (e.g. M for Orange).`,
    hint_proceed: `Press Space or Enter to start!`,
    hint_next: `Press Space or Enter to proceed, or Esc to quit.`,
    warning_alt: `Alt combined with some other keys (e.g. Tab) may cause undesired behavior (e.g. switching windows).`,
    warning_ctrl: `Ctrl combined with some other keys (e.g. W) may cause undesired behavior (e.g. closing the tab).`,
    warning_mouse: `Make sure to keep the mouse cursor inside the browser window; otherwise the game may lose focus.`,
});
